# =============================================================================
# EMAIL CAMPAIGN APP - ENVIRONMENT CONFIGURATION
# =============================================================================

# -----------------------------------------------------------------------------
# APPLICATION SETTINGS
# -----------------------------------------------------------------------------
# Environment: development, staging, production
ENVIRONMENT=development

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_RELOAD=true

# CORS Settings (comma-separated list of allowed origins)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Debug mode
DEBUG=true

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION
# -----------------------------------------------------------------------------
# Local development uses SQLite
DATABASE_URL=sqlite:///./email_campaigns.db

# Production PostgreSQL (Railway)
# DATABASE_URL=postgresql://username:password@hostname:port/database_name

# Database pool settings
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10

# -----------------------------------------------------------------------------
# GOOGLE SHEETS API CONFIGURATION
# -----------------------------------------------------------------------------
# Path to Google Service Account JSON file
GOOGLE_CREDENTIALS_FILE=credentials.json

# Alternative: Use environment variables for credentials (preferred for production)
# GOOGLE_CREDENTIALS_JSON={"type":"service_account","project_id":"..."}

# Google Sheets API settings
GOOGLE_SHEETS_SCOPES=https://www.googleapis.com/auth/spreadsheets

# -----------------------------------------------------------------------------
# SMTP EMAIL CONFIGURATION (SpaceMail)
# -----------------------------------------------------------------------------
# SMTP Server Settings
SMTP_HOST=smtp.spacemail.com
SMTP_PORT=587
SMTP_USE_TLS=true
SMTP_USE_SSL=false

# Email Authentication
SMTP_USERNAME=your-email@yourdomain.com
SMTP_PASSWORD=your-app-password

# Default sender information
DEFAULT_FROM_EMAIL=your-email@yourdomain.com
DEFAULT_FROM_NAME=Your Name

# Email rate limiting (emails per minute)
EMAIL_RATE_LIMIT=60

# -----------------------------------------------------------------------------
# SECURITY SETTINGS
# -----------------------------------------------------------------------------
# Secret key for JWT tokens and session management
SECRET_KEY=your-super-secret-key-change-this-in-production

# JWT Token settings
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=30

# API Rate limiting
API_RATE_LIMIT=100

# -----------------------------------------------------------------------------
# LOGGING CONFIGURATION
# -----------------------------------------------------------------------------
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Log format
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# Log file path (optional, logs to console if not set)
# LOG_FILE=logs/app.log

# -----------------------------------------------------------------------------
# CAMPAIGN SETTINGS
# -----------------------------------------------------------------------------
# Maximum number of recipients per campaign
MAX_RECIPIENTS_PER_CAMPAIGN=1000

# Campaign timeout in minutes
CAMPAIGN_TIMEOUT_MINUTES=60

# Retry settings for failed emails
EMAIL_RETRY_ATTEMPTS=3
EMAIL_RETRY_DELAY_SECONDS=300

# -----------------------------------------------------------------------------
# DEVELOPMENT SETTINGS
# -----------------------------------------------------------------------------
# Enable API documentation (FastAPI Swagger UI)
ENABLE_DOCS=true

# Enable API testing endpoints
ENABLE_TEST_ENDPOINTS=true

# Mock email sending for testing (set to true to prevent actual emails)
MOCK_EMAIL_SENDING=false

# -----------------------------------------------------------------------------
# MONITORING & ANALYTICS
# -----------------------------------------------------------------------------
# Enable request logging
ENABLE_REQUEST_LOGGING=true

# Enable performance monitoring
ENABLE_PERFORMANCE_MONITORING=false

# External monitoring service API key (optional)
# MONITORING_API_KEY=your-monitoring-service-key

# -----------------------------------------------------------------------------
# RAILWAY PRODUCTION SETTINGS (for deployment)
# -----------------------------------------------------------------------------
# These will be automatically set by Railway
# PORT=8000
# RAILWAY_STATIC_URL=https://your-app.railway.app
# DATABASE_URL=postgresql://...

# -----------------------------------------------------------------------------
# OPTIONAL: REDIS CONFIGURATION (for caching and job queues)
# -----------------------------------------------------------------------------
# Redis URL for caching and background jobs
# REDIS_URL=redis://localhost:6379/0

# Cache TTL in seconds
# CACHE_TTL=3600

# -----------------------------------------------------------------------------
# OPTIONAL: SENTRY ERROR TRACKING
# -----------------------------------------------------------------------------
# Sentry DSN for error tracking in production
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# -----------------------------------------------------------------------------
# BACKUP & STORAGE SETTINGS
# -----------------------------------------------------------------------------
# Backup configuration
# BACKUP_ENABLED=false
# BACKUP_S3_BUCKET=your-backup-bucket
# BACKUP_S3_ACCESS_KEY=your-s3-access-key
# BACKUP_S3_SECRET_KEY=your-s3-secret-key